#summary Relases.

= JTestMe: Relases =

== 1.1 ==
 * Corregido executor webservice
 * Añadida posibilidad de asignar system properties ({{{System.getProperty()}}}) y classpath para los valores de las propiedades de los executors.

== 1.0 ==
 * Versión inicial del proyecto
 * Implementado el módulo core (jtesme-core).
 * Implementado el módulo test-webapp (jtestme-test-webapp).
 * Creado los executors: connection, datasource, graphics, jndi, jdbc, ldap, openoffice, smtp, webservice y custom.
 * Creada los views: html, xml, text y json.